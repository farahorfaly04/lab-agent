# Lab Platform Device Agent Configuration
# Copy this file to .env and update with your specific values

# Device Settings (override config.yaml values)
DEVICE_ID=lab-device-01
DEVICE_LABELS=lab,production

# MQTT Broker Settings (REQUIRED - these override config.yaml)
MQTT_HOST=10.205.10.7
MQTT_PORT=1883
MQTT_USERNAME=mqtt
MQTT_PASSWORD=your-mqtt-password-here

# Features Directory (REQUIRED - path to features repository)
FEATURES_PATH=../features